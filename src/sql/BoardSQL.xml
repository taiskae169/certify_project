<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<select id="getCount" resultType="int">
		select count(*) from Board;
	</select>
	<select id="getArticle" parameterType="map" resultType="board.vo.BoardVO">
  		select * from (select num,writer,subject,content,passwd,orgname,newname,step,rownum r from (select * from Board order by num desc) order by num desc) 
  		<![CDATA[where r >= #{start} and r <= #{end}]]> 
  	</select>
</mapper>