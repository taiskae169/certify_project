<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="certi">
	
  	<select id="cer_category" resultType="certify.vo.Cer_CategoryVO">
  		select * from cer_category
  	</select>
  	
  	<select id="getAllCertify" resultType="certify.vo.CertifyVO">
  		select * from CERTIFY_INFO
  	</select>
  	
  	<select id="getCertify_Board" resultType="certify.vo.CertifyVO">
  		select * from (select num,cate,type,content,name,rownum r from (select * from CERTIFY_INFO where num in #{type} order by num asc) order by num asc) 
  		<![CDATA[where r >= #{start} and r <= #{end}]]> 
  	</select>
  	
  	<select id="getCertify_Board_Spec" resultType="certify.vo.CertifyVO">
  		select * from (select num,cate,type,content,name,rownum r from (select * from CERTIFY_INFO  where num in #{type} and name like #{cer_name} order by num asc) order by num asc) 
  		<![CDATA[where r >= #{start} and r <= #{end}]]> 
  	</select>
  	
  	<select id="getSpecCertify" resultType="certify.vo.CertifyVO">
  		select * from CERTIFY_INFO where num=#{num}
  	</select>	
  	
  	<select id="getAllCertifyOrder" resultType="certify.vo.CertifyVO">
  		select * from CERTIFY_INFO order by num asc
  	</select>
  	
  	<select id="getCount_gukki" resultType="int">
		select count(*) from CERTIFY_INFO where num in #{type}
	</select>
  	
</mapper>